[project]
name = "t1d-companion"
version = "2.0.0"
description = "Glucose forecasting companion (educational)"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.0", "httpx>=0.25"]
