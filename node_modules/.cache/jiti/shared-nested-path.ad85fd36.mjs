"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.MAX_NESTED_PATH_ENTRIES = void 0;exports.encodeNestedPathEnv = encodeNestedPathEnv;exports.isSafeNestedPathId = isSafeNestedPathId;exports.parseNestedPathEnv = parseNestedPathEnv;exports.sanitizeNestedPath = sanitizeNestedPath;var path = _interopRequireWildcard(await jitiImport("node:path"));function _interopRequireWildcard(e, t) {if ("function" == typeof WeakMap) var r = new WeakMap(),n = new WeakMap();return (_interopRequireWildcard = function (e, t) {if (!t && e && e.__esModule) return e;var o,i,f = { __proto__: null, default: e };if (null === e || "object" != typeof e && "function" != typeof e) return f;if (o = t ? n : r) {if (o.has(e)) return o.get(e);o.set(e, f);}for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);return f;})(e, t);}

const MAX_NESTED_ID_LENGTH = 128;
const MAX_NESTED_PATH_ENTRIES = exports.MAX_NESTED_PATH_ENTRIES = 4;



function isSafeNestedPathId(value) {
  return typeof value === "string" &&
  value.length > 0 &&
  value.length <= MAX_NESTED_ID_LENGTH &&
  !path.isAbsolute(value) &&
  !value.includes("/") &&
  !value.includes("\\") &&
  !value.includes("..");
}

function finiteNumber(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : undefined;
}

function nonEmptyString(value, max) {
  return typeof value === "string" && value.length > 0 ? value.slice(0, max) : undefined;
}

function sanitizeNestedPath(value) {
  if (!Array.isArray(value)) return [];
  return value.map((part) => {
    if (!part || typeof part !== "object") return undefined;
    const record = part;
    if (!isSafeNestedPathId(record.runId)) return undefined;
    return {
      runId: record.runId,
      ...(finiteNumber(record.stepIndex) !== undefined ? { stepIndex: finiteNumber(record.stepIndex) } : {}),
      ...(nonEmptyString(record.agent, 128) ? { agent: nonEmptyString(record.agent, 128) } : {})
    };
  }).filter((part) => Boolean(part)).slice(0, MAX_NESTED_PATH_ENTRIES);
}

function parseNestedPathEnv(value) {
  if (!value) return [];
  try {
    return sanitizeNestedPath(JSON.parse(value));
  } catch {
    return [];
  }
}

function encodeNestedPathEnv(value) {
  const sanitized = sanitizeNestedPath(value);
  return sanitized.length ? JSON.stringify(sanitized) : "";
} /* v9-dbf19448d0c94118 */
